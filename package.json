{
  "private": true,
  "name": "test-realm",
  "scripts": {
    "build": "worktop build src/index.ts",
    "types": "tsc --noEmit --skipLibCheck"
  },
  "dependencies": {
    "realm-web": "^2.0.0"
  },
  "devDependencies": {
    "typescript": "4.4.3",
    "worktop.build": "0.0.3"
  }
}
